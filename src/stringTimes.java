public String stringTimes(String str, int n)
{
    String stringTimesOutput = "";
    for (int i = 0; i < n; i++)
    {
        stringTimesOutput += str;  // could use += here
    }
    return stringTimesOutput;
}